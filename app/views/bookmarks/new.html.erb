<h1>Add a movie to <%= @list.name %></h1>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment, placeholder: 'Please leave a comment' %>
  <%= f.association :movie, prompt: 'Select a movie' %>
  <%= f.submit class: 'btn btn-flat' %>
<% end %>
